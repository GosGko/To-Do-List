<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.2/css/all.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant:wght@600&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles/main.css">
    <title>ToDoList</title>
</head>
<body>
   
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/" method="post"><i class="far fa-calendar-alt"></i>Today</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/work"><i class="far fa-clone"></i>Work</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <h1 id="heading">🍂To Do List🍂</h1>
  <div><% if (locals.date) { %>
    <h1 id="aftheading" class="box"><%= date %></h1>
   <% }else{ %>
    <h1 id="aftheading" class="box">Work List</h1>
  <% } %>
  </div>
  <div class="box">
   <!-- form, text input and submit button that when dettects click make new div with checkbox and paragraph with the text that the user typed in the text input.-->
   <form class="item" action="<% if (locals.date)  { %> / <% } else { %> /work <% } %>" method="post">
   <input type="text" name="newItem" placeholder="New Item" autocomplete="off" required>
   <button type="submit" name="list" value="<% if (locals.date)  { %> <% date %> <% } else { %> workday <% } %>">+</button>
  </form>
    <div>
      <% if(locals.toDoListDay) { %>
        <% for(var i = 0 ; i < toDoListDay.length ; i++) { %>
    <div class="todo">
      <input type="checkbox" name="checkbox" id="checkbox">
      <p class="libs">
        <%= toDoListDay[i] %>
      </p>
  </div>
      <% } %>
      <% } %>
      <% if(locals.toDoListWork) { %>
        <% for(var i = 0 ; i < toDoListWork.length ; i++) { %>
    <div class="todo">
      <input type="checkbox" name="checkbox" id="checkbox">
      <p class="libs">
        <%= toDoListWork[i] %>
      </p>
  </div>
      <% } %>
      <% } %>
  </div>
  </div>
    <p class="kon">Copyright © <%= year%></p>
</body>
</html>